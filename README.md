# KuCoin Trading Bot

Simple trading bot skeleton for KuCoin Spot using Python.

## Usage
1. Copy `.env.example` to `.env` and fill credentials.
2. Install deps: `pip install -r requirements.txt`.
3. Run `bash run_bot.sh` to start in tmux.
